[phases.setup]
nixPkgs = ["nodejs_24"]

[phases.install]
cmds = ["corepack enable", "corepack prepare pnpm@11.7.0 --activate", "pnpm i --frozen-lockfile"]

[start]
cmd = "HOST=0.0.0.0 PORT=4321 node ./dist/server/entry.mjs"
