services:
    database:
        image: "postgres:17"
        ports:
            - 5432:5432
        env_file:
            - .env
        networks:
            - postgres-network
        volumes:
            - db-data:/var/lib/postgresql/data/

networks:
    postgres-network:
        driver: bridge

volumes:
    db-data:
